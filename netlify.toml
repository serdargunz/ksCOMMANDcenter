[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Single-page app: route everything that isn't a real file or function to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
